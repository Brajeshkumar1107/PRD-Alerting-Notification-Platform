spring:
  application:
  name: PRD-Alerting-&-Notification-Platform

  datasource:
    url: jdbc:mysql://localhost:3306/prd
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # options: none, validate, update, create, create-drop
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080


# Use a strong, random JWT secret (replace with a secure value in production)
security:
  jwt:
    secret: 7b8f9e2c4d1a6e5f3b2c1d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4
    expirationMinutes: 60
